enum Verb{
  write,
  put,
  end_line,
  add,
  subtract,
  multiply,
  divide,
  if_cond,
  elseif_cond,
  else_cond,
  end_cond,
  set,
  create,
  for_loop,
  unknown_verb};

enum Identifier{ to, equals, unknown_identifier };